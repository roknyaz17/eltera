# Выжимка за 25 июня 2026

Итого за день: **~12 400 строк** в основном патче (`35905b0`) + модуль аутентификации (`34da04b`).
Реально нового функционала — 6 крупных подсистем: биллинг, вакансии+HH, рефералы, email-аутентификация, профиль организации, метрики.

---

## 1. Аутентификация по email-кодам (`34da04b`)

Полностью переделанный flow входа/регистрации через одноразовые коды:

- **Регистрация**: `POST /register` → `register/resend` → `register/verify` (3 шага с подтверждением кода из письма)
- **Вход**: `POST /login` (шлёт код) → `login/resend` → `login/verify` (выдаёт токены)
- **Токены**: `refresh`, `logout` (с отзывом refresh-токена), `me`
- Сервис `auth_challenges.py` (169 стр.) — логика «челленджей»: генерация, TTL, лимиты повторной отправки, верификация
- Две миграции: `auth_email_codes`, `email_challenges`
- Переработан `core/security.py`
- **Тесты**: `test_auth_email_verification.py` (250 стр.) — покрытие всего сценария
- Фронт: контроллер `auth.js`, экраны ввода кода в `render.js`, превью `auth-preview.html`

## 2. Биллинг и платежи через Moneta (`35905b0`)

Полноценный платёжный модуль:

- **Эндпоинты**: баланс/пакеты/история, создание пополнения (`topup`), статус платежа, отмена, симуляция оплаты (для тестов), списание оценок (`debit`), callback от Moneta
- `services/billing.py` (423 стр.) — бизнес-логика баланса и пакетов
- `services/moneta.py` (158 стр.) — интеграция с платёжкой: подпись запросов (MD5), сборка параметров, формирование URL оплаты, верификация подписи нотификаций, ответные подписи
- Модели/схемы биллинга, миграция `billing_moneta`
- Доки: `moneta-payments.md`

## 3. Вакансии + интеграция HeadHunter (`35905b0`)

Самый большой кусок — `services/vacancies.py` (736 стр.), `routes/vacancies.py` (265 стр.):

- Список вакансий с метриками за период
- KPI-карточки + drill-down по каждой карточке
- Импорт выбранных вакансий из HH, синхронизация HH-вакансий организации
- Назначение теста по умолчанию на вакансию, список доступных тестов
- Конвертация отклика в кандидата
- По каждой вакансии: метрики, отклики, воронка подбора, кандидаты
- История вакансий HH (миграция `hh_vacancy_history`), доработка `services/hh.py`
- Спека `vacancies-hh-spec.md` (441 стр.) + `vacancies-hh-implementation.md`

## 4. Реферальная программа (`35905b0`)

- **Эндпоинты**: сводка, трата бонусов на оценки, начисление бонуса рефереру при оплате, заявка на вывод
- `services/referrals.py` (259 стр.), модели/схемы, две миграции (`referral_program`, `referral_invited_org`)

## 5. Профиль организации (`35905b0`)

- Получение/обновление реквизитов компании (`GET`/`PATCH`)
- Модель, схема, миграция `organization_profile`

## 6. Ассессмент — таймер и напоминания (`35905b0`)

- Таймер прохождения (миграция `assessment_timer`)
- `assessment_reminders.py` (134 стр.) — авто-напоминания по незавершённым/открытым ссылкам с «дозреванием»
- Существенная переработка `assessment_flow.py` (+247)

## 7. Наблюдаемость / Observability (`35905b0`)

- `observability/metrics.py` (185 стр.) — метрики Prometheus: события ассессмента, длительность теста, invite→open, AI-скоринг (кол-во/время), AI-расходы (токены, стоимость в рублях), реферальные операции, логины
- Дашборд Grafana `eltera-tier-a.json`, конфиги `prometheus.yml`, datasources/dashboards
- `docker-compose.yml` для ops-стека

## 8. Фронтенд (`35905b0`)

Крупная переработка: `app.js` (+746), `render.js` (+1044), `styles.css` (+635), новый модуль `domain/filtering.js`, расширение `api.js` под все новые эндпоинты.

## 9. В работе (незакоммичено)

Рефакторинг email-шаблонов:

- Общая функция `render_email()` в `email.py` — табличная вёрстка для совместимости с почтовыми клиентами, градиентный хедер/кнопка, preheader, опциональная кнопка
- `auth_email.py` переведён на новый шаблон + блок `_code_block` с крупным разрядным отображением одноразового кода
